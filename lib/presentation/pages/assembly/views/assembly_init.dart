import 'package:flutter/material.dart';

class AssemblyInitPage extends StatelessWidget {
  const AssemblyInitPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Hola')),);
  }
}